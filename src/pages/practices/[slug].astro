---
import { getCollection, render } from "astro:content";
import BaseLayout from "../../layouts/BaseLayout.astro";
import SaveButton from "../../components/SaveButton.astro";
import ShareButtons from "../../components/ShareButtons.astro";

export async function getStaticPaths() {
  const practices = await getCollection("practices", ({ data }) => !data.draft);
  return practices.map((entry) => ({ params: { slug: entry.id }, props: { entry } }));
}

const { entry } = Astro.props;
const { Content } = await render(entry);
const description = entry.data.subtitle ?? `${entry.data.title} — תרגול מדיטציה מתוך "ללא מאמץ"`;

const articleJsonLd = {
  "@context": "https://schema.org",
  "@type": "Article",
  headline: entry.data.title,
  description,
  inLanguage: "he-IL",
  url: `https://zen.matanbrown.com/practices/${entry.id}/`,
};

const breadcrumbJsonLd = {
  "@context": "https://schema.org",
  "@type": "BreadcrumbList",
  itemListElement: [
    { "@type": "ListItem", position: 1, name: "ללא מאמץ", item: "https://zen.matanbrown.com/" },
    { "@type": "ListItem", position: 2, name: "תרגולים", item: "https://zen.matanbrown.com/practices/" },
    { "@type": "ListItem", position: 3, name: entry.data.title, item: `https://zen.matanbrown.com/practices/${entry.id}/` },
  ],
};
---

<BaseLayout title={entry.data.title} description={description} jsonLd={[articleJsonLd, breadcrumbJsonLd]} image={entry.data.coverImage}>
  <div class="title-row">
    <h1>{entry.data.title}</h1>
    <SaveButton collection="practices" id={entry.id} lang="he" />
  </div>
  {entry.data.subtitle && <p class="eyebrow">{entry.data.subtitle}</p>}
  {entry.data.coverImage && (
    <img class="cover" src={entry.data.coverImage} alt={entry.data.coverImageAlt || entry.data.title} loading="eager" fetchpriority="high" decoding="async" />
  )}
  <div class="prose">
    <Content />
  </div>
  <ShareButtons title={entry.data.title} lang="he" />

  <p><a href="/practices/">← כל התרגולים</a></p>
</BaseLayout>

<style>
  .cover { width: 100%; border-radius: 4px; margin-block: var(--space-3); }
</style>
